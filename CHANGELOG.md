# CHANGELOG.md

## 0.2.0 - 2026-05-03

- 실배포 안정화 관점으로 추가 점검을 수행했다.
- Windows 관리자 프로그램 안에 `도움말/매뉴얼` 메뉴를 추가해 초기 Google 설정, QR 생성, 조회, 출력, 문제 해결을 바로 볼 수 있게 했다.
- `admin-desktop/scripts/package_installer.ps1`을 추가해 버전명이 들어간 배포 ZIP과 단일 설치 EXE를 Downloads에 생성할 수 있게 했다.
- 최초 Apps Script 설정에 `createInitialSetupKey` 기반 일회성 초기 설정 키를 추가해 배포 URL 선점 위험을 줄였다.
- 모바일 제출 bootstrap 응답에서 내부 settings hash, Drive folder id, submit token hash가 내려가지 않도록 공개 필드만 반환하게 변경했다.
- Apps Script 증분 동기화 기준 시각에 30초 안전 여유를 둬 초 단위 경계 레이스로 기록이 누락될 가능성을 줄였다.
- Google Sheet 저장과 XLSX 출력에 수식 주입 방어를 추가했다.
- Desktop 설정 업로드에 마지막 동기화 시각 기반 충돌 감지를 추가해 오래된 로컬 설정이 Google 설정을 덮어쓰는 위험을 줄였다.
- 설정 화면의 연결 테스트/업로드/다운로드를 백그라운드 작업으로 전환해 Google 응답 지연 시 UI가 멈춰 보이지 않도록 개선했다.
- QR 부착용 HTML의 QR 이미지 경로를 file URI로 생성하고 실명 HTML escape 처리를 추가했다.
- Google에서 내려받은 학교명을 데스크톱 헤더/출력 설정에 반영하도록 보강했다.
- Windows 관리자 프로그램의 Desktop Sync Key를 로컬 설정 파일에 저장할 때 Windows DPAPI로 현재 사용자 계정에 묶어 보호하도록 변경했다.
- Windows 관리자 프로그램의 로컬 QR submit token도 DPAPI 보호값으로 저장하도록 보강했다.
- Google 동기화 기준 시각을 PC 로컬 시간이 아니라 Apps Script 서버가 내려주는 `next_since` 값으로 저장하도록 변경해 경계 시점 누락 가능성을 줄였다.
- Apps Script 제출 검증을 강화했다: 담당자/당직자 실제 등록 여부, 항목 상태 정규화, 특이사항/클라이언트 정보 길이 제한, 첨부파일 수 제한, MIME/signature 검증.
- Apps Script Drive 권한 스코프를 실제 폴더 생성/첨부 저장에 맞게 조정했다.
- 모바일 제출 버튼 중복 클릭과 서버 레이스를 막기 위해 클라이언트 재진입 방지와 서버 LockService를 추가했다.
- 미전송 임시 저장은 24시간 만료 정책을 두고, 저장 공간 부족 시 사용자에게 안내하도록 변경했다.
- 첨부파일 업로드 실패 시 제출 레코드가 먼저 저장되어 재전송이 꼬이지 않도록, 파일 생성 성공 후 제출/첨부 행을 기록하는 순서로 조정했다.
- Windows 관리자 프로그램의 Google 동기화를 UI 스레드 밖에서 실행해 학교망 지연 시 화면이 멈춰 보이는 문제를 줄였다.
- QR 생성 전 Apps Script URL과 로컬 submit token을 확인해 더미/죽은 QR 출력물을 만들지 않도록 차단했다.
- 출력물은 사용자 추가 점검항목까지 반영하고, 실명에 Windows 파일명 금지 문자가 있어도 저장되도록 보강했다.
- Apps Script 템플릿 include와 파라미터 주입을 안전한 helper로 정리하고, 관리자 화면의 동적 HTML 생성을 DOM 기반으로 보강했다.
- PyInstaller 배포 ZIP을 다시 생성하고 exe smoke 테스트를 수행했다.

## 0.1.9 - 2026-04-28

- 배포 전 QA/보안 리뷰를 수행하고 학교 전달용 ZIP 생성 흐름을 `admin-desktop/scripts/package.ps1` 하나로 정리했다.
- 패키징 스크립트가 pytest, PyInstaller 빌드, 필수 파일 검증, Downloads 배포 폴더/ZIP 생성을 한 번에 처리하도록 보강했다.
- 관리자 웹 CSV 다운로드에서 관리자 토큰 fallback 값이 URL query string에 남지 않도록 `google.script.run` + Blob 다운로드 방식으로 변경했다.
- Google 배포 권한, 점검자 무로그인 리스크, Desktop Sync Key 보관/재발급 안내를 문서에 보강했다.
- 최종 배포 ZIP smoke 테스트와 실행파일 시작 확인을 수행했다.

## 0.1.8 - 2026-04-27

- 점검기록과 출력/보관 화면의 시작일/종료일 날짜 입력, 실/상태 선택 상자의 폭과 간격을 정리했다.
- 달력 드롭다운 버튼과 콤보박스 드롭다운 버튼을 같은 톤의 작은 버튼 스타일로 맞췄다.
- 출력/보관 화면의 출력물 미리보기를 오른쪽 좁은 영역에서 하단 대형 영역으로 옮겼다.
- `표시순서`가 무엇을 의미하는지 실/사람/점검항목 관리 화면 안내문에 예시까지 추가했다.

## 0.1.7 - 2026-04-27

- 전역 폰트 확대 배율을 낮추고 좌측 메뉴, 상단 제목, 대시보드 카드/타일 글자 크기를 실무 화면에 맞게 줄였다.
- 실/담당자/당직자/점검항목 관리의 `순서` 표기를 `표시순서`로 바꾸고 낮은 숫자가 먼저 보인다는 안내를 추가했다.
- 프로그램 `설정` 화면 안에 Google Sheet/Apps Script 최초 설정 방법 10단계 안내를 추가했다.
- 최종 조합형 GUI 미리보기 HTML도 축소된 글자 크기에 맞춰 갱신했다.
- Windows PowerShell에서 한글 실행파일 이름이 깨질 수 있어 PyInstaller 패키징 스크립트의 앱 이름 처리를 보강했다.

## 0.1.6 - 2026-04-26

- A2 큰 버튼 UI, C1 행정실 데스크형, B3 출력 중심 관리자 시안을 조합해 전체 관리자 프로그램 UI를 재정리했다.
- 상단 헤더에 학교명, Google 연결 상태, 수동 동기화 버튼을 추가했다.
- 좌측 메뉴를 어두운 사이드바 스타일로 변경하고 선택 상태를 더 명확하게 했다.
- 대시보드 빠른 이동 버튼을 큰 타일형으로 변경했다.
- 출력/보관 미리보기 영역을 종이 문서 느낌으로 보이도록 다듬었다.
- 최종 조합형 GUI 미리보기 HTML을 추가했다.

## 0.1.5 - 2026-04-26

- 출력/보관 화면의 상단 여백을 줄여 제목과 조회 영역이 더 위에 보이도록 조정했다.
- 좌측 메뉴 글자를 추가로 50% 키워 20pt로 적용했다.
- 실 관리, 담당자/당직자 관리, 점검항목 관리에 삭제/복구 버튼을 추가했다.
- 삭제는 실제 행 삭제가 아니라 미사용 처리로 동작하게 하여 실수로 인한 데이터 유실을 줄였다.

## 0.1.4 - 2026-04-26

- 목업 1~5의 장점을 합친 통합 대시보드형 행정 UI를 실제 관리자 프로그램에 반영했다.
- 대시보드에 최초 사용 단계, 실별 상태판, 우선 확인 목록을 추가했다.
- 실/담당자/당직자/점검항목 관리 화면에 상단 직접 입력 영역과 안내문을 추가했다.
- 출력/보관 화면을 조회 목록과 출력물 미리보기 분할 화면으로 개편했다.
- QR 모바일 점검표 로컬 미리보기 HTML을 추가했다.
- Apps Script 모바일 점검표 스타일도 큰 버튼/큰 글씨 기준으로 조정했다.

## 0.1.3 - 2026-04-26

- Windows 관리자 프로그램 전역 폰트를 기존보다 50% 크게 적용했다.
- 큰 글씨에 맞춰 기본 창 크기와 좌측 메뉴 폭을 확대했다.
- 실 관리, 담당자/당직자 관리, 점검항목 관리 테이블이 창 너비를 꽉 채우도록 수정했다.
- 관리 화면에서 내부 ID/hash 열은 숨기고 사람이 보는 열 중심으로 정리했다.
- 선택용 UI 화면 목업 5종 HTML을 추가했다.

## 0.1.2 - 2026-04-26

- Google 관리자 웹에 시작일/종료일/실/상태 필터를 추가했다.
- Google 관리자 웹 CSV 다운로드가 현재 조회 조건을 반영하도록 수정했다.
- Google 관리자 웹 상세보기에서 첨부파일 링크를 확인할 수 있게 했다.
- 데스크톱 관리자 확인과 이상 없음 일괄확인이 Desktop Sync Key로 Google Sheet 원본에도 반영되도록 보완했다.

## 0.1.1 - 2026-04-26

- 대시보드에 초보자용 빠른 이동 버튼과 최초 사용 순서를 추가했다.
- 점검기록 조회를 기간별, 실별, 상태별 필터로 확장했다.
- 출력/보관 화면을 기간별, 실별 출력이 가능하도록 확장했다.
- QR 저장 폴더와 출력물 저장 폴더를 설정 화면에서 선택할 수 있게 했다.
- 로컬 실/담당자/점검항목 설정을 Google Sheet로 업로드하는 버튼을 추가했다.
- 연결 테스트가 Apps Script URL뿐 아니라 Desktop Sync Key까지 검증하도록 변경했다.
- 앱 시작 후와 지정 주기마다 Google 데이터를 자동 동기화하도록 보완했다.
- Apps Script 초기 설정이 완료된 뒤에는 관리자 인증 없이 재초기화되지 않도록 보호했다.

## 0.1.0 - 2026-04-26

- 기존 Flask/SQLite QR보안점검표를 Google Apps Script + Google Sheet/Drive 중간서버형 구조로 재설계했다.
- 모바일 무로그인 제출, 관리자 웹 확인, Windows 관리자 프로그램 동기화 구조를 구현했다.
- 이상 없음 일괄확인과 이상 있음 개별확인 정책을 코드와 문서에 반영했다.
- PySide6 기반 관리자 프로그램, SQLite migration, QR 생성, Excel/HTML 출력, HWP fallback을 추가했다.
- 초보자용 배포/설정/문제해결 문서와 테스트 코드를 추가했다.
