# 실사용 준비 진행 결과 — 2026-09-10 KST

현재 판정: 기존 테스트 웹앱 v12의 정상 제출·관리자 조회·개별확인은 실제 확인했다. 신규 학교 자동 설치와 일반 학교 출시 검증은 아직 완료되지 않았다.

## 최신 설치센터 구성 확인 (이전 미설정 기록보다 우선)

- 전용 Cloud 프로젝트의 Drive·Sheets·Apps Script API 활성화와 공개 웹 client ID 반영을 확인했다.
- 사용자 승인 후 홈페이지·개인정보 URL·승인 도메인을 저장했다. 개인정보 페이지는 `installer/privacy/index.html`로 추가했다.
- 외부 OAuth 앱은 TESTING이며 소유자 테스트 계정 1개가 등록되어 있다. 계정 이메일은 이 공개 문서에 기록하지 않는다.
- 기존 코드가 요청하는 6개 scope만 저장했다: openid, userinfo.email, userinfo.profile, drive.file, script.projects, script.deployments. 콘솔 분류는 비민감 4개·민감 2개·제한 0개였다. 실제 계정의 접근 동의는 별도다.
- 소스 `release-data.js`는 미게시 기본값을 유지한다. 운영용 데이터는 clean 런타임 manifest 검증을 통과한 빌드에서만 생성한다.
- 실제 18파일 런타임 패키징에서 정적 파일용 비밀 검사기가 정상 `adminToken` 변수 읽기와 URL 검증용 정규식을 차단하는 오류를 재현했다. 비밀 검사를 끄지 않고 디코딩된 런타임 검사와 정확한 생성 래퍼 검증을 분리했다. 실제 clean 런타임으로 25파일·런타임18파일 빌드가 통과했다.
- 개인정보 페이지 작성: Muse `opencode/muse-spark-1.3-contributor-free`, 세션 `ses_f777201e7ffegOevdo17FcSrYh`, 합성 요구사항만 전달. 빌드 검사 수정 초안: 같은 모델, 세션 `ses_f776e4080ffeUoXv0mLIWUCXE3`, 공개 빌드 도구 및 공개 런타임 일부만 전달. CLI xhigh를 명시했지만 공급자 내부 추론 수준은 독립 검증하지 못했다.
- Codex가 초안 검사에서 줄바꿈 뒤 하드코딩 fallback과 scope URL 접두어 허용 문제를 발견하여 실패하는 회귀 테스트 2개를 추가했다. Muse 후속 세션 `ses_f7767ec34ffeAAaRr3kX5RZh8e`가 이를 수정했다. Codex가 실제 운영 UUID 생성식의 추가 오탐을 실패 테스트로 재현하고 해당 두 표현만 좁게 허용했다. 새 검사 테스트 20개와 설치센터·설치·업데이트·GAS 전체 Node 테스트가 통과했다.
- 최초 후속 호출은 테스트의 합성 private-key 표식 때문에 송신 필터가 차단했고 해당 테스트 파일은 외부에 보내지 않았다. 이후 CLI 경로 미지정 시 모델 조회가 실패했으며 실제 exe 경로를 지정한 같은 무료 모델 호출은 성공했다. 유료/다른 모델 fallback은 없었다.
- 로컬 1440px·390px에서 6개 화면과 개인정보 페이지의 h1/직접 경로/가로 넘침/페이지 JS 오류를 검사했다. 이 화면 검사는 Google 연결이나 신규 학교 설치 성공 증거가 아니다.
- 게시 데이터 포함 화면에서는 업데이트 페이지의 40자 커밋 표시가 390px에서 가로 넘침을 일으켰다. Muse 세션 `ses_f77626593ffeaUPY2WgBUb0tjK`가 업데이트 HTML의 범위 한정 줄바꿈 CSS만 추가했고 Codex가 검토·병합했다. 재실행에서 1440/390px의 7개 경로 모두 넘침 없음·페이지 JS 오류 없음, 개인정보 화면, 모바일 메뉴 열기/Escape 닫기, 도움말 검색/0건, 데모 등록→자동 상세→닫기→관리자 목록 3건→상세 열기/닫기를 확인했다.
- 게시 maker는 실제 GIS 스크립트를 로드해 `Google 연결 준비가 되었습니다`를 표시했다. 빈 입력으로 공간 만들기를 누르면 입력 안내를 표시하고 완료 버튼은 차단됐다. 이는 로그인·Google 자원 생성 성공을 의미하지 않는다. QA 스크립트의 기존 생성 버튼 disabled 가정과 데모 등록 후 자동 상세 닫기 누락을 실제 동작에 맞게 수정했으며 운영 동작을 검사에 맞춰 바꾸지 않았다.

## 코드와 로컬 검증

- 코드 커밋: `0ced31a`.
- `Submit.gs`: 같은 요청번호의 digest 불일치는 `[SUBMISSION_REVIEW_REQUIRED]` 오류로 유지하며 관리자 확인을 안내한다. 구 digest를 성공으로 인정하거나 기존 기록을 재해시하지 않는다.
- `Client.js.html`: 확인 필요 요청은 기존 payload/요청번호/보관기한을 가진 `review_required` 임시 보관 항목으로 표시하고 자동 재전송에서 제외한다. 저장 공간 오류가 나도 관리자 확인 안내는 표시한다. 저장 실패 시 새로고침 이후 표시 유지까지 보장하는 것은 아니다.
- 확인 필요 자료의 보관기한 만료 안내를 일반 네트워크 실패와 구분한다.
- 회귀 테스트: 수정 전 서버 3건·화면 5건 실패 재현. 수정 후 제출/화면 27개 테스트 통과.
- 설치센터·설치·업데이트·GAS 전체 Node 검사 통과(dot reporter), `git diff --check` 통과. 이번 전체 실행의 정확한 테스트 수는 별도로 집계하지 않았다.
- clean 런타임 빌드: `dirty=false`, 공개 12파일·관리자 18파일. 일반 학교용 릴리스를 발행한 것은 아니다.

## Google 배포 및 실제 확인

- 기존 테스트 deployment ID를 유지하여 v11 → v12 갱신. v11 복구 버전 보존.
- 원격 편집본이 v11과 동일함을 확인한 후 백업했다. 기존 manifest와 접근 설정(`ANYONE_ANONYMOUS`, `USER_DEPLOYING`) 변경 없음.
- 원격 고정 v12 18파일과 현재 로컬 소스의 정규화 비교 불일치 0건.
- 배포 직후 GET에 이전 버전이 반환되어 즉시 검증이 실패했으나, 후속 독립 GET으로 v12 활성 상태를 확인했다. 재배포를 반복하지 않았다.
- 기존 합성 기록 2건과 3개 장소가 관리자 화면에 유지됨. 초기 설정·QR 재발급·기존 기록 삭제 없음.
- 합성 정상 점검 1건을 브라우저에서 제출: `rec_mtu8j2rr_yz4g1m7q`.
- 관리자 상세에서 동일 기록번호·비고·5개 정상 항목·`COMMITTED`를 확인했다. 총 3건 조회.
- 이 신규 합성 기록에만 관리자 개별확인과 합성 메모를 저장했고 목록의 `확인` 상태를 확인했다. 기존 2건은 미확인 상태 유지.
- 쿠키/인증 없는 HTTP 관리자 조회는 `ok=false`, `data=null`, Google 계정 확인 필요 오류로 거부됨.
- CLI 토큰으로 Sheets 직접 조회는 HTTP 403. 권한을 확대하지 않았고 이번 Sheet 직접 대조를 PASS로 기록하지 않는다. 관리자 상세를 통한 동일 기록 확인은 별도로 PASS다.

## 홈페이지·설치센터

### 2026-09-10 후속 확인

- 홈페이지 HTTPS 요청이 인증서 검증을 통과하여 HTTP 200을 반환했다.
- GitHub Pages `Enforce HTTPS`를 활성화했다. HTTP 요청은 `301` 및 `Location: https://qr-safe.h19h19.com/`을 반환했다.
- 따라서 아래 인증서 미발급 기록은 이전 점검 이력이며 현재 HTTPS 차단 사유는 해소됐다.
- Google Cloud에서 `QR-check Installer` 프로젝트 생성 화면을 준비했다. 생성 가능 할당량 2개가 표시되어 신규 생성/기존 프로젝트 사용 선택을 사용자에게 요청했다. 프로젝트 생성·OAuth 클라이언트 생성·추가 접근 권한 부여는 아직 수행하지 않았다.

### 이전 점검 이력

- `qr-safe.h19h19.com`: GitHub Pages DNS check successful 확인. HTTPS는 인증서 미발급 상태이고 인증서 검증 오류가 계속되어 미완료다.
- 설치센터 OAuth client ID는 공란, 소스 release는 `unpublished`다. 설치 성공을 임의로 표시하지 않는다.
- 기존 clasp 인증·테스트 Google 배포는 확인했다. 현재 Google Cloud 프로젝트 목록에서 QR 설치센터 전용 프로젝트를 식별하지 못했다. 다른 용도의 OAuth 클라이언트를 재사용하지 않았다.
- 신규 학교 자동 설치는 전용 OAuth 클라이언트/허용 출처, Google 권한 승인, 릴리스 구성과 실제 설치 E2E 검증이 필요하다.

## 아직 확인하지 않은 항목

- v12 실제 사진/PDF 신규 업로드 및 Drive 권한 재검증(과거 v11 증거와 구분).
- 실제 휴대폰/인쇄 QR, 교육기관 Workspace, 미등록 Google 계정, 실제 rollback.
- live 동일 요청 멱등/충돌 및 구버전 재전송. 이번에는 로컬 회귀 검증만 수행.
- 설치센터 차단/권한 철회 후 독립 운영, 신규 학교 자동 설치 E2E.
- 이번 작업은 실제 학교 개인정보를 사용한 운영 검증이 아니다.

## 실행 주체와 범위

### 설치센터 후속 작업 (2026-09-10)

#### OAuth 클라이언트 생성 후 추가 확인

- 이전의 약관 확인 대기 기록은 과거 이력이다. Apps Script API 사용 설정됨, OAuth 브랜딩 생성됨을 확인했다. 사용자 승인 후 웹 OAuth 클라이언트를 생성했다.
- 허용 JavaScript 원본은 `https://qr-safe.h19h19.com` 하나, 리디렉션 URI는 없다. 공개 client ID만 로컬 소스 설정에 반영했으며 client secret은 소스·배포물·Muse에 전달하지 않았다.
- 인증/권한 안전성/릴리스 차단 관련 3개 테스트 파일이 통과했고, 정적 빌드는 24파일 unpublished로 통과했다. 실제 로그인·학교 생성·운영 재배포는 이번 확인에 포함하지 않는다.
- 콘솔은 테스트 사용자 0명, OAuth 구성 미완료를 표시했다. 브랜딩의 홈페이지·개인정보처리방침·승인된 도메인이 비어 있어 후속 구성이 필요하다. scope 등록과 테스트 사용자 추가도 아직 남았다.
- 간단한 운영 안내 문서는 Muse `opencode/muse-spark-1.3-contributor-free`에 공개 문서와 비밀 없는 상태만 전달해 정리했다. 세션 `ses_f7784dacbffeeqs1GyCbEZThIO`, CLI xhigh 옵션. Codex가 결과를 검토하고 새 로컬 client ID 반영 상태를 보정했다. 공급자 내부 추론 수준을 독립적으로 확인하지는 못한다.

- 사용자 승인으로 Cloud 프로젝트 `QR-check Installer` (`qr-check-installer`)를 실제 생성했다. Drive / Sheets API 사용 설정 완료를 콘솔에서 확인했다. 기존 학교 런타임과 다른 Cloud 프로젝트 설정은 변경하지 않았다.
- Apps Script API는 이용약관 확인을 위해 사용자에게 화면을 넘겼다. 아직 활성화하지 않았다.
- OAuth 앱 이름·외부 테스트 대상·소유자 연락처를 입력했다. 사용자 데이터 정책 동의 및 최종 생성은 사용자 대기다. OAuth client ID는 여전히 공란이며 신규 학교 설치는 아직 검증되지 않았다.
- Muse가 공개 maker HTML·디자인 토큰만 받아 `maker-polish.css`를 생성하고 HTML 링크를 추가했다. 실제 운영 JS·Google 인증·상태 머신·배포 URL·기존 디자인 CSS는 변경하지 않았다.
- 모델: OpenCode `opencode/muse-spark-1.3-contributor-free`, 명시적 CLI `--variant xhigh`. 설치된 runner와의 차이를 검토한 로컬 임시 사본을 사용했으며 전역 설정은 변경하지 않았다. 공급자 내부 추론 수준은 독립적으로 확인하지 못한다. 세션 `ses_f77908eb6fferIUZXwB5suX0oU`.
- Codex가 CSS 및 HTML diff를 검토하고 빌드 allowlist·정확한 파일 수 회귀 테스트를 병합했다. 새 링크 테스트 실패를 먼저 확인한 후 관련 4개 테스트 파일을 실행해 통과했다. 빌드 결과는 24파일, unpublished였다.
- 로컬 Playwright에서 1440×960 / 390×960 화면, 4개 안내 구역, 가로 넘침 없음, JS/console 오류 없음, 모바일 메뉴 열기/닫기, 미연결 입력·설치 버튼 차단을 확인했다. 초기 QA 스크립트의 입력 시도는 정상적인 disabled 폼 때문에 실패했으며, 실제 미연결 상태에 맞게 검사를 수정했다. 폼 잠금을 풀거나 Google 성공을 모의 처리하지 않았다. Google 스크립트 네트워크는 이 화면 검사에서 제외했다.
- 변경 화면은 로컬 빌드 검증까지만 수행했다. 이 CSS 변경의 운영 홈페이지 재배포는 아직 하지 않았다. 기존 v12 학교 웹앱은 변경하지 않았다.

- 코드·테스트 작성: OpenCode CLI, `opencode/muse-spark-1.3-contributor-free`. 공개 코드와 합성 테스트만 전달. 계정·QR 토큰·학교 데이터·clasp 인증은 전달하지 않음.
- 주요 Muse 세션: `ses_f79509d37ffeMzlk817iwSA6Em`(서버/화면), `ses_f794e3433ffeYSfdTIfkOXlbvD`(화면 테스트). xhigh 실제 적용은 이 실행에서 확인하지 못했으므로 주장하지 않는다.
- 첫 작업의 잘못된 절대경로 접근은 거부되어 변경 없이 끝났고, 상대경로 사용을 명시한 재실행으로 해결했다. 첫 구현은 단계 제한으로 화면 테스트가 미완료라 별도 Muse 작업으로 보강했다.
- Codex가 diff 검토, 기존 테스트 보존, VM 배열 비교 보정, 로컬 테스트·배포·브라우저 검증을 수행했다. Muse는 테스트나 배포를 직접 실행하지 않았다.
